import { createNativeFormRenderer } from "@resistdesign/voltra/native";

/**
 * Native form-suite reference example.
 *
 * Purpose:
 * - Show the default native suite entrypoint.
 * - Keep form rendering usage separate from layout examples.
 */
export const nativeFormSuiteExample = createNativeFormRenderer();
