import { createWebFormRenderer } from "@resistdesign/voltra/web";

/**
 * Web form-suite reference example.
 *
 * Purpose:
 * - Show the default web suite entrypoint.
 * - Show where to start when wiring AutoField/AutoForm in a web app.
 */
export const webFormSuiteExample = createWebFormRenderer();
