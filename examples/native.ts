import {
  createNativeFormRenderer,
  makeNativeEasyLayout,
} from "@resistdesign/voltra/native";

export const nativeRendererExample = createNativeFormRenderer();
export const nativeLayoutExample = makeNativeEasyLayout("header, 1fr");
