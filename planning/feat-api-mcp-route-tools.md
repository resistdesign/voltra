# Feature: API MCP Route Tools

- [ ] Add protocol-correct MCP tool hosting that composes directly into a Voltra RouteMap.
- [ ] Reuse normal Route auth for the whole MCP endpoint and expose tool handler/handlerFactory access to normalized caller context.
- [ ] Preserve MCP HTTP status, headers, and body through the existing cloud-function router.
- [ ] Add focused tests and a consumer example.
- [ ] Verify tests/build/exports and open the PR.
