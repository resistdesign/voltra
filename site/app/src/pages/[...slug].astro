---
import { App } from "../client/App";
import BaseLayout from "../layouts/BaseLayout.astro";

// DEV ONLY: This ensures all routes are served as static HTML for SPA behavior if not prerendered differently
export const prerender = import.meta.env.PROD;
---

<BaseLayout>
  <section>
    <App client:only="react" />
  </section>
</BaseLayout>
