---
import { AdvancedDemo } from "./_components/AdvancedDemo";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Voltra Form Gen - Advanced Demo</title>
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen,
          Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
        padding: 2rem;
        max-width: 1200px;
        margin: 0 auto;
        background: #fdfdfd;
      }
      h1 { color: #333; margin-bottom: 0.5rem; }
      p { color: #666; margin-bottom: 2rem; }
      
      :global(.demo-grid) {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 2rem;
        height: 80vh;
      }

      :global(.pane) {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        background: white;
        padding: 1rem;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.05);
        border: 1px solid #eee;
        overflow: hidden;
      }

      :global(.pane-header) {
        font-weight: 600;
        color: #444;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      :global(.editor-container) {
        flex: 1;
        border: 1px solid #ddd;
        border-radius: 4px;
        overflow: hidden;
      }

      :global(.preview-container) {
        flex: 1;
        overflow-y: auto;
        padding-right: 0.5rem;
      }

      :global(button) {
        background: #007bff;
        color: white;
        border: none;
        padding: 0.5rem 1rem;
        border-radius: 4px;
        cursor: pointer;
        font-weight: 500;
      }
      :global(button:hover) { background: #0056b3; }
      :global(button:disabled) { background: #ccc; cursor: not-allowed; }

      :global(select) {
        padding: 0.5rem;
        border-radius: 4px;
        border: 1px solid #ccc;
        min-width: 150px;
      }

      :global(pre) {
        background: #f1f3f5;
        padding: 1rem;
        border-radius: 4px;
        font-size: 0.85rem;
        overflow-x: auto;
      }
    </style>
  </head>
  <body>
    <h1>Advanced Form Generation Demo</h1>
    <p>Edit the TypeScript interface on the left to generate a form on the right.</p>

    <!-- Use client:only="react" to ensure strict client-side rendering without hydration mismatches -->
    <AdvancedDemo client:only="react" />

  </body>
</html>
